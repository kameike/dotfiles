#!/bin/sh
#vim: syntax=sh

exit 0

echo "pppp"

set -eu

echo "test0"

ls $HGROGE

echo "test1"

set -e
set +e
git -C ~/.config add --all || echo "nothing to add"
git -C ~/.config commit -m"upadte" || echo "nothing to update"
git -C ~/.config pull origin master 
git -C ~/.config push origin HEAD 
set -e

source ~/.bashrc
