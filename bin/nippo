#!/bin/sh
#vim: syntax=sh

set -eu
 
# open 'https://www.notion.so/kameike/e53e0adc006541fc8cad0576d73dff87'


date=`date '+%y-%m-%d'`
path="$NIPPO_PATH/$date.md"

if test ! -f "$path"; then
  echo "# $date" >> "$path"
fi

time=`date +"%k:%M"`

echo "## $time"  >> "$path"


open "$path"
